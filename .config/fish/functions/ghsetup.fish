function ghsetup -d 'Sets up a new GitHub repo. -p = private'
    # maybe look into properly parsing flags with fish?
    # sets up a github repo, asking for repo name and if private
    # then copies returned repo address to clipboard in clone command
end
